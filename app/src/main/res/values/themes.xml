<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.POS" parent="Theme.MaterialComponents.DayNight.NoActionBar"> /* Gets rid of the action bar on the screen. */
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/shopColour</item>
        <item name="colorPrimaryVariant">@color/shopColour</item>
        <item name="colorOnPrimary">@color/shopColour</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>